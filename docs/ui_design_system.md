# UI Design

The native PyGame interfaces use a dark road-focused palette with cyan for the
SARSA-controlled car and active controls. Their purpose is direct interaction:
configure traffic, drive with arrows, pause, change pace, and save evidence.

The FastAPI-served browser frontend uses the same dark visual direction for
training control and analytics. SARSA (`S`) uses the established cyan accent
`#00E5FF`.
