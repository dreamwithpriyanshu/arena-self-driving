# Streamlit Community Cloud dashboard dependencies.
# Native HighwayEnv/PyGame simulation dependencies are intentionally excluded:
# Cloud only serves the dashboard and does not create a simulation window.
streamlit>=1.30,<2.0
pandas>=2.1,<3.0
altair>=5.2,<6.0
